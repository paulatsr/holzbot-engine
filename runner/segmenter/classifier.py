from __future__ import annotations

from pathlib import Path

from runner.utils import logging as runner_logging


def classify(plan: Path) -> None:
    runner_logging.log(f"Classifier placeholder pentru {plan}")
